import 'package:get/state_manager.dart';
import 'package:get_storage/get_storage.dart';

class StorageController extends GetxController {
  final GetStorage authBox = GetStorage('Auth');
  final box = GetStorage();
  void dcd(){
  
  }
}
